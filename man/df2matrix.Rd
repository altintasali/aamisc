% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/df2matrix.R
\name{df2matrix}
\alias{df2matrix}
\title{Convert \code{data.frame} to \code{matrix} while moving the non-numeric values into \code{rownames}}
\usage{
df2matrix(df, col2rowname = c("new_groups", "groups"), sep = "_")
}
\arguments{
\item{df}{A \code{data.frame}}

\item{col2rowname}{Vector for the columns to be moved to rownames. Can be numeric (column ids) or character (column names)}

\item{sep}{Seperator for rownames if more than 1 element supplied in \code{col2rowname}}
}
\value{
A matrix object.
}
\description{
Convert \code{data.frame} to \code{matrix} while moving the non-numeric values into \code{rownames}
}
\examples{
# Create sample data.frame
x <- data.frame(Group1 = letters[1:5], ids = 1:5, id.squared = (1:5)^2)
x
X <- df2matrix(x)
X

# Create sample data.frame with 2 character columns
x2 <- data.frame(x, Group2 = LETTERS[6:10])
x2

X2 <- df2matrix(x, col2rowname = c(1,4), sep = "_")
X2
X2 <- df2matrix(x, col2rowname = c("Group1","Group2), sep = "_")
X2

# Rownames can be reverse ordered
X3 <- df2matrix(x, col2rowname = c(1,4), sep = "_")
X3
X3 <- df2matrix(x, col2rowname = c("Group1","Group2), sep = "_")
X3

}
